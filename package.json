{
  "name": "jev-sift",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin: filters tool results through Jev before they reach the model.",
  "type": "module",
  "scripts": { "build": "./build.sh" },
  "dependencies": { "@typesafe-ai/sdk": "^0.6.0", "dotenv": "^17.4.2" },
  "devDependencies": { "esbuild": "^0.25.0", "tsx": "^4.23.13", "typescript": "^7.0.2" }
}
